// El array alumno tiene:
// en la primera posicion el nombre
// en la segunda posicion el apellido
// en la tercera posicion la edad

let alumno = ["Banu", "Trawally", 19]
    // En la consola poner el nombre
console.log(alumno[0]);
// En la consola de error poner el apellido
console.error(alumno[1])
    // En un alert, poneis la edad
alert(alumno[2])

console.log(alumno.length)

let alumnoObjeto = {
    "nombre": "Banu",
    "apellido": "Trawally",
    "edad": "19"
}

console.log(alumnoObjeto.nombre)
console.error(alumnoObjeto.apellido)
alert(alumnoObjeto.edad)